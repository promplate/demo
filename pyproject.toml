"""
This file manages the dependencies for the Promplate Demo project and specifies the required Python version.
"""
[project]
authors = [{ name = "Muspi Merol", email = "me@promplate.dev" }]
dependencies = []
requires-python = ">=3.10"
readme = "README.md"
license = { text = "MIT" }
