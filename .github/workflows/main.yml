name: GitHub Actions

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Run pnpm install
        run: pnpm install

    - name: Install pnpm
        run: npm install -g pnpm
